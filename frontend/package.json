{
  "name": "mcp-cloud-frontend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev --host 0.0.0.0 --port 3000",
    "dev:debug": "NODE_OPTIONS='--max-old-space-size=4096' nuxt dev --host 0.0.0.0 --port 3000",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "test": "vitest",
    "clean": "rm -rf .nuxt .output node_modules/.vite",
    "reinstall": "npm run clean && npm install"
  },
  "devDependencies": {
    "@nuxt/devtools": "^2.6.2",
    "@nuxtjs/tailwindcss": "^6.10.0",
    "@playwright/test": "^1.55.0",
    "@vitejs/plugin-vue": "^5.0.0",
    "@vue/test-utils": "^2.4.6",
    "happy-dom": "^18.0.1",
    "nuxt": "^3.10.0",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "@pinia/nuxt": "^0.11.2",
    "marked": "^9.0.0",
    "pinia": "^2.1.7",
    "vue": "^3.4.0",
    "xterm": "^5.3.0",
    "xterm-addon-fit": "^0.8.0"
  }
}
