# AWS 환경 설정 파일
# 이 파일은 aws-setup-helper.sh에 의해 자동 생성되었습니다.
# 생성 시간: Sat Sep 27 11:50:46 KST 2025
#
# ⚠️  보안 경고: 이 파일에는 민감한 정보가 포함될 수 있습니다.
# 실제 배포 시에는 이 파일을 .gitignore에 추가하고 안전하게 관리하세요.

# AWS 계정 정보 (실제 값으로 교체 필요)
AWS_ACCOUNT_ID=""
AWS_USER_ARN=""

# AWS 리전 및 네트워크 설정 (실제 값으로 교체 필요)
REGION="ap-northeast-2"
VPC_ID=""
SUBNET_ID=""

# 환경 변수 내보내기
export AWS_DEFAULT_REGION="$REGION"
export AWS_REGION="$REGION"
export AWS_VPC_ID="$VPC_ID"
export AWS_SUBNET_ID="$SUBNET_ID"
