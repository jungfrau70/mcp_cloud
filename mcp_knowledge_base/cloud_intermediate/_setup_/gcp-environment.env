# GCP 환경 설정 파일
# 이 파일은 gcp-setup-helper.sh에 의해 자동 생성되었습니다.
# 생성 시간: Sat Sep 27 12:19:45 KST 2025
#
# ⚠️  보안 경고: 이 파일에는 민감한 정보가 포함될 수 있습니다.
# 실제 배포 시에는 이 파일을 .gitignore에 추가하고 안전하게 관리하세요.

# GCP 계정 정보 (실제 값으로 교체 필요)
GCP_ACCOUNT=""
GCP_PROJECT_ID=""

# GCP 리전 및 존 설정
REGION="asia-northeast3"
ZONE="asia-northeast3-a"

# 환경 변수 내보내기
export GOOGLE_CLOUD_PROJECT="$GCP_PROJECT_ID"
export GCP_PROJECT="$GCP_PROJECT_ID"
export GCP_REGION="$REGION"
export GCP_ZONE="$ZONE"
