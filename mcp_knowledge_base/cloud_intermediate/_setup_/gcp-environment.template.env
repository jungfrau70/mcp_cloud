# GCP 환경 설정 템플릿 파일
# 이 파일을 복사하여 gcp-environment.env로 이름을 변경하고 실제 값으로 교체하세요.

# GCP 계정 정보 (실제 값으로 교체 필요)
GCP_ACCOUNT=""
GCP_PROJECT_ID=""

# GCP 리전 및 존 설정
REGION="asia-northeast3"
ZONE="asia-northeast3-a"

# 환경 변수 내보내기
export GOOGLE_CLOUD_PROJECT="$GCP_PROJECT_ID"
export GCP_PROJECT="$GCP_PROJECT_ID"
export GCP_REGION="$REGION"
export GCP_ZONE="$ZONE"

# 보안 설정
# GCP 자격 증명은 다음 방법 중 하나로 설정하세요:
# 1. gcloud CLI: gcloud auth login
# 2. 서비스 계정 키: export GOOGLE_APPLICATION_CREDENTIALS="path/to/key.json"
# 3. Application Default Credentials (ADC)
# 4. Workload Identity (GKE에서 권장)

# 중요: 이 파일에는 실제 GCP 자격 증명을 포함하지 마세요!
# 서비스 계정 키 파일은 안전한 위치에 보관하고 .gitignore에 추가하세요.
