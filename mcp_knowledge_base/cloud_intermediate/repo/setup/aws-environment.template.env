# AWS 환경 설정 템플릿 파일
# 이 파일을 복사하여 aws-environment.env로 이름을 변경하고 실제 값으로 교체하세요.

# AWS 계정 정보 (실제 값으로 교체 필요)
AWS_ACCOUNT_ID=""
AWS_USER_ARN=""

# AWS 리전 및 네트워크 설정 (실제 값으로 교체 필요)
REGION="ap-northeast-2"
VPC_ID=""
SUBNET_ID=""

# 환경 변수 내보내기
export AWS_DEFAULT_REGION="$REGION"
export AWS_REGION="$REGION"
export AWS_VPC_ID="$VPC_ID"
export AWS_SUBNET_ID="$SUBNET_ID"

# 보안 설정
# AWS CLI 자격 증명은 다음 방법 중 하나로 설정하세요:
# 1. AWS CLI: aws configure
# 2. 환경 변수: export AWS_ACCESS_KEY_ID="your-key"
# 3. IAM 역할: EC2 인스턴스에 IAM 역할 할당
# 4. AWS SSO: aws sso login

# 중요: 이 파일에는 실제 AWS 자격 증명을 포함하지 마세요!
# AWS_ACCESS_KEY_ID와 AWS_SECRET_ACCESS_KEY는 환경 변수나 AWS CLI 설정을 통해 관리하세요.
