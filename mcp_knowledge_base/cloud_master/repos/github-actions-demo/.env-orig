# 🔧 환경 변수 설정 예시
# 실제 사용 시 .env 파일로 복사하여 사용하세요

# 애플리케이션 설정
NODE_ENV=development
PORT=3000
HOST=0.0.0.0

# 데이터베이스 설정
DB_HOST=localhost
DB_PORT=5432
DB_NAME=github_actions_demo
DB_USER=postgres
DB_PASSWORD=password

# Redis 설정
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# JWT 설정
JWT_SECRET=your-super-secret-jwt-key-here
JWT_EXPIRES_IN=24h

# Docker Hub 설정
DOCKER_USERNAME=your-dockerhub-username
DOCKER_PASSWORD=your-dockerhub-password

# GitHub 설정
GITHUB_TOKEN=your-github-token
GITHUB_REPOSITORY=your-username/github-actions-demo

# 모니터링 설정
PROMETHEUS_URL=http://localhost:9090
GRAFANA_URL=http://localhost:3001

# 알림 설정
SLACK_WEBHOOK_URL=your-slack-webhook-url
DISCORD_WEBHOOK_URL=your-discord-webhook-url

# AWS 설정 (선택사항)
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_REGION=us-east-1

# AWS EC2 VM 배포 설정
AWS_VM_HOST=your-aws-ec2-ip
AWS_VM_USERNAME=ubuntu
AWS_VM_SSH_KEY=../cloud-scripts/cloud-deployment-key.pem

# GCP 설정 (선택사항)
GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account.json
GCP_PROJECT_ID=your-gcp-project-id

# GCP Compute Engine VM 배포 설정
GCP_VM_HOST=your-gcp-vm-ip
GCP_VM_USERNAME=ubuntu
GCP_VM_SSH_KEY=../cloud-scripts/cloud-deployment-key
GCP_VM_ZONE=us-central1-a

# 프로젝트 설정
PROJECT_NAME=github-actions-demo
PROJECT_VERSION=1.0.0
DOCKER_IMAGE_NAME=github-actions-demo
DOCKER_TAG=latest

# GitHub 설정
GITHUB_USERNAME=your-github-username
GITHUB_REPO_NAME=github-actions-demo

# 애플리케이션 설정
APP_PORT=3000
APP_HOST=0.0.0.0
