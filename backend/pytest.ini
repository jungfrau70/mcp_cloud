[pytest]
# Local backend pytest config so running from within backend/ recognizes markers
markers =
    live: tests that hit a running live backend service
    integration: integration-level tests (may hit external services or multiple components)
    unit: fast unit tests
    slow: slow tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -q
