절차서를 `.md` 파일로 만들려면 [ChatGPT에 로그인](https://chat.openai.com)하셔야 합니다.
로그인 후에는 Markdown 형식 문서를 생성하거나 편집할 수 있는 **ChatGPT Canvas** 기능을 사용할 수 있습니다.

대신 지금 여기서 Markdown 문서 형태로 내용 드릴게요. 복사해서 직접 `.md` 파일로 저장하시면 됩니다:

---

````markdown
# 📘 gotra 데이터베이스 백업 및 복구 절차서 (Windows → Ubuntu 환경 전환 고려)

## 🗂️ 기본 정보

- **DB 이름**: `gotra`
- **DB 사용자**: `postgres`
- **Docker Compose 파일**: `docker-compose-database-win.yml`
- **PostgreSQL 컨테이너 이름**: `postgres`
- **백업 파일 이름**: `gotra_backup.sql`

---

## ✅ 1. 백업 절차 (Windows / Ubuntu 동일 방식)

```bash
docker exec -t postgres pg_dump -U postgres gotra > gotra_backup.sql
````

* STDOUT 방식으로 OS 간 호환성 높음
* 인코딩 명시적으로 설정 (옵션):

```bash
docker exec -t postgres pg_dump -U postgres --encoding=UTF8 gotra > gotra_backup.sql
```

---

## ✅ 2. 백업 파일 이관 (Windows → Ubuntu)

### 방법 1: Windows PowerShell → WSL 공유 경로

```powershell
copy gotra_backup.sql \\wsl$\Ubuntu\home\youruser\
```

또는 USB, SCP, Git 등을 사용해 파일 이동

---

## ✅ 3. 복구 절차 (Ubuntu)

### 3-1. 기존 gotra DB 삭제 (옵션)

```bash
docker exec -it postgres psql -U postgres -c "DROP DATABASE IF EXISTS gotra;"
docker exec -it postgres psql -U postgres -c "CREATE DATABASE gotra;"
```

### 3-2. 복구 실행 (STDIN 방식)

```bash
cat gotra_backup.sql | docker exec -i postgres psql -U postgres gotra
```

---

## ✅ 4. 운영체제 변경 시 고려사항 요약

| 항목     | Windows                 | Ubuntu (Linux) | 해결 방안                              |
| ------ | ----------------------- | -------------- | ---------------------------------- |
| 줄바꿈    | CRLF (\r\n)             | LF (\n)        | 문제시 `dos2unix gotra_backup.sql` 사용 |
| 인코딩    | UTF-8 / CP1252          | UTF-8          | `--encoding=UTF8` 명시               |
| 경로     | `C:\path\to\file`       | `/home/user/`  | 상대 경로 사용                           |
| 퍼미션    | 낮음                      | 높음             | STDIN/STDOUT 방식 사용 권장              |
| 볼륨 마운트 | Docker Desktop → WSL 기반 | 직접 파일 시스템 사용   | 마운트보다 명령어 활용 권장                    |

---

## ✅ 전체 명령어 요약

| 작업        | 명령어                                                                             |
| --------- | ------------------------------------------------------------------------------- |
| 📤 백업     | `docker exec -t postgres pg_dump -U postgres gotra > gotra_backup.sql`          |
| 🧹 DB 삭제  | `docker exec -it postgres psql -U postgres -c "DROP DATABASE IF EXISTS gotra;"` |
| 🏗️ DB 생성 | `docker exec -it postgres psql -U postgres -c "CREATE DATABASE gotra;"`         |
| 📥 복구     | `cat gotra_backup.sql \| docker exec -i postgres psql -U postgres gotra`        |

---

## 📁 예시 시나리오 (Windows → Ubuntu)

1. Windows에서 `gotra_backup.sql` 파일 생성
2. USB, SCP 또는 공유 폴더를 통해 Ubuntu로 파일 이관
3. Ubuntu에서 복구 명령 실행

---

**작성일**: 2025-08-11
**작성자**: *(작성자 이름 입력)*

```

---

복사해서 원하는 에디터(예: VS Code, Notepad++, nano 등)로 `.md` 파일로 저장하시면 됩니다.  
원하는 포맷이나 내용 추가가 있으면 말씀해주세요!
```
